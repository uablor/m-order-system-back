/**
 * Permissions are generated from controllers via PermissionGeneratorService (run in seed.ts).
 * No static permission list here.
 */

export async function runPermissionSeeder(): Promise<void> {
  // No-op: permissions are created by PermissionGeneratorService.generateFromControllers()
  return;
}
